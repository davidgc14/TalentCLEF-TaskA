%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% DISCUSION
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Discusión}
\fancyhead[RE]{\textsc{CAPÍTULO} \thechapter. Discusión}
\label{ch:Discusion}

\noindent BORRADOR. Este capítulo analiza y discute en profundidad los resultados obtenidos en la evaluación presentada en el capítulo anterior. 

Voy a realizar un análisis crítico, tanto de análisis de resultados como de análisis de errores y decisiones tomadas.

A pesar de las restricciones computacionales y de tiempo, se ha logrado desarrollar un sistema competitivo para la \gls{Tarea}, alcanzando una posición destacada en la clasificación general. Esto demuestra la eficacia de la arquitectura del sistema propuesto, que combina múltiples modelos y técnicas de fusión y re-ranking para maximizar el rendimiento.

Sin embargo, es importante reconocer las limitaciones del enfoque adoptado. La dependencia de modelos preentrenados puede introducir sesgos inherentes a los datos de entrenamiento originales, lo que podría afectar la generalización del sistema a nuevos dominios o lenguajes. Además, la complejidad computacional de los modelos utilizados limita la escalabilidad del sistema para aplicaciones en tiempo real o con grandes volúmenes de datos.

En cuanto a la selección de fuentes para los modelos de recuperación, a pesar de intentar abarcar un rango amplio de modelos entrenados en muchos campos distintos, hay un sesgo hacia el MTEB leaderboard, que, a pesar de ser un referente, no deja de ser una fuente sesgada y limitada de información. Otro punto complejo ha sido la búsqueda de modelos preentrenados exclusivamente en español, ya que la mayoría de modelos disponibles están entrenados en inglés o en múltiples idiomas, lo que puede afectar el rendimiento en tareas específicas del español. No se ha encontrado ningún modelo contrastivo entrenado exclusivamente en español, lo que ha obligado a utilizar modelos multilingües que pueden no estar optimizados para las particularidades del idioma.

En cuanto a la figura \ref{fig:zero_shot_map}, se observa una clara predominancia de los modelos multilingües\footnote{Cabe destacar que la información de en cuántos lenguajes se había entrenado viene dada por el último entrenamiento realizado, pero muchos de ellos son ajustes finos de modelos más generales, por lo general entrenados para multilingüe, reduciendo aun más la fiabilidad de las etiquetas monolingües que se muestran en la gráfica.}. Los monolingües quedan en un claro segundo puesto, pudiendo estar motivado por la falta de generalización de los modelos exclusivamente monolingües, o quizás por la presencia de anglicismos en múltiples términos de títulos de trabajos presentes hoy en día en el idioma español.

Continuando con la figura, se pueden observar también dos peculiaridades en su distribución. La primera es que hay muchos que están alineados en vertical, lo que indica que muchos modelos comparten la misma arquitectura base y, por tanto, es probable que compartan también limitaciones y sesgos similares (luego comentaremos por qué, para los tres modelos elegidos para la fusión, hay dos que tienen la misma arquitectura base e igual no fue una buena decisión). La segunda es que, tal y como reportó la organización de la tarea en \cite{gasco2025overview}, los modelos parecen presentar un punto dulce en cuanto a tamaño, a partir del cual modelos más grandes presentan empeoramientos en el rendimiento de la tarea. Este punto se puede ver empíricamente entre 500M y 1000M de parámetros, donde se observa un pico en el rendimiento y, a partir de ahí, los modelos más grandes tienden a tener un rendimiento inferior. Esto puede deberse a varios factores, discusión en la que no voy a entrar.

Luego se eligieron los 10 mejores para realizar un ajuste fino. Está claro que la estrategia de entrenamiento muestra que no se podía ajustar a muchas épocas debido al sobreajuste, pero quizás ya con una época, que es lo que hemos entrenado, ya se ha sobreajustado (a pesar de que hayan subido las métricas de validación). Además, haber elegido solo los 10 mejores modelos puede haber limitado la diversidad de enfoques y arquitecturas, lo que podría haber afectado la capacidad del sistema para capturar diferentes aspectos de la tarea. Un ejemplo es el modelo \textit{Alibaba-NLP/gte-multilingual-base}, que aunque se encontraba en el puesto 9 en la evaluación zero-shot, tras el ajuste fino no logró mejorar su rendimiento, posiblemente debido a su arquitectura o a la naturaleza de los datos de entrenamiento utilizados; esto lo colocó en el tercer puesto, haciendo que formara parte del sistema final. Quizás se podría haber ampliado la lista a la que se aplicó el ajuste fino para asegurar que modelos que a priori no estaban en el top 10, pero que podrían haber mejorado significativamente tras el ajuste fino, no fueran descartados prematuramente. Relacionado con el ajuste fino, la mejora no ha sido muy sustancial, lo que sugiere que los modelos preentrenados ya estaban bastante bien adaptados a la tarea o que los datos de entrenamiento disponibles no eran suficientes para lograr mejoras significativas. 

En relación con los datos de entrenamiento, quizás hubiera sido interesante haber investigado acerca de fuentes de datos con los que alimentar el conjunto de datos de entrenamiento, aportando mayor diversidad y cantidad de ejemplos. Esto podría haber ayudado a los modelos a generalizar mejor y a capturar una gama más amplia de variaciones en los títulos de trabajo.

En cuanto a la selección de modelos para la fusión, se seleccionaron los tres mejores, pero dos de ellos compartían la misma arquitectura base (\textit{??}). Esto podría haber limitado la diversidad de enfoques y perspectivas en la fusión, ya que los modelos con arquitecturas similares tienden a cometer errores similares. Una estrategia alternativa podría haber sido seleccionar modelos con arquitecturas más diversas, incluso si su rendimiento individual era ligeramente inferior, para maximizar la complementariedad entre ellos.

Otro sesgo que también tenemos en la fusión es que se han seleccionado solo tres modelos y a partir de ahí se ha realizado la fusión. Quizás hubiera sido interesante haber explorado combinaciones de más modelos o haber probado varias combinaciones de los modelos ya ajustados para ver si alguna combinación alternativa hubiera podido mejorar el rendimiento final. En cualquier caso, la mejora de la fusión no ha llegado ni a un punto porcentual, mostrando que quizás la estrategia de fusión no ha sido la más adecuada o que los modelos seleccionados no eran lo suficientemente complementarios entre sí.

Para la fase de reranking se ha intentado proceder como con los modelos de retrieval: hacer una evaluación masiva de modelos para poder seleccionar con mejor criterio el modelo cross-encoder. Sin embargo, en este caso no contamos con un ranking público de mejores modelos para la tarea (MTEB tiene fundamentalmente bi-encoders y modelos densos). Ya al hacer el retrieval me di cuenta de que en Hugging Face no todos los modelos son etiquetados de forma correcta (bi-encoders, cross-encoders, o modelos de retrieval, modelos de ranking), produciendo una visión sesgada en la búsqueda de modelos, ya que solo voy a tener acceso a los que sí hayan etiquetado correctamente su modelo. Es por ello que el número de modelos conseguidos, 90, no es ni de lejos representativo con todos los que deberían aparecer ahí fuera. 

También es cierto que el haber decidido trabajar con sentence-transformers ha limitado en gran medida el acceso a los modelos disponibles.

He tratado en el reranking de obtener primero una visión general del rendimiento y adecuación de los modelos a la tarea, viendo su comportamiento en la reordenación de distintas cantidades: 5, 10, 25, 50 y 100. Esto ha mostrado que los reordenamientos de valores grandes empeoraban los resultados ya conseguidos con el ajuste fino y la fusión. Por ello, al ver que la mayoría de los modelos mejoraba la métrica de la fusión con una reordenación de un top pequeño, se decidió hacer un análisis más detallado de cómo de acusada era la degradación. En la figura \ref{fig:top10_reranking_individual} se ve cómo parece que, para la tarea en cuestión, los modelos en general tienden a empeorar la métrica cuando k es mayor o igual a 20. 

Sorprende aquí mucho el comportamiento de modelos como posih-reranker-base-mse, que a pesar de presentar la peor métrica para k=5, es el que menos degradación sufre cuando k aumenta. Un comportamiento similar presenta mmarco-mMiniLMv2-L12-H384-v1, que en este caso incluso es el único que mejora su rendimiento para k=10, aunque luego se degrade igualmente. Este último es el que resultados más estables reporta, siendo una buena opción a considerar si se buscara generalización. Debido a que solo se buscaba la maximización de la métrica MAP, es por ello que se ha seleccionado el mejor simplemente, aunque es probable que esta decisión haya repercutido en la métrica sobre el conjunto de test.

% \noindent En este capítulo se analizan en detalle los resultados obtenidos en la evaluación presentada en el capítulo anterior. Se discuten las principales tendencias observadas, se interpretan las mejoras conseguidas en cada etapa del proceso y se examinan las posibles causas de desviaciones o comportamientos inesperados. Además, se resaltan las implicaciones de estos resultados, así como las limitaciones del enfoque propuesto y las oportunidades de mejora identificadas.

% \section{Resultados de la evaluación Zero-Shot}

% En la primera fase de evaluación se analizaron 190 modelos preentrenados sin ajuste específico en la tarea en cuestión. Los resultados obtenidos muestran que los modelos multilingües superan consistentemente a los monolingües en nuestro problema concreto. Aunque los corpus que estamos utilizando son exclusivamente en español, los modelos entrenados en múltiples idiomas demostraron una mayor capacidad de generalización semántica. Esto podría deberse a que los títulos profesionales en español suelen incorporar anglicismos y términos técnicos procedentes del inglés, de modo que los modelos multilingües captan mejor estos vocablos especializados.

% En la dispersión del rendimiento \gls{MAP} frente al tamaño del modelo se observa un patrón destacable: existen agrupaciones verticales que reflejan la presencia de varios modelos con la misma arquitectura base. Esta alineación vertical indica que aquellos modelos comparten mecanismos de representación similares y, por tanto, tienden a presentar limitaciones y sesgos comunes. Este aspecto resulta relevante para la fusión de modelos, ya que dos de los tres modelos finalmente elegidos compartían la misma arquitectura base (ambos derivados de MPNet), lo que podría haber reducido la diversidad de enfoques en la combinación.

% Asimismo, se aprecia que los modelos de tamaño intermedio (aproximadamente entre 500M y 1000M de parámetros) alcanzan los mejores valores de \gls{MAP}. Más allá de este rango, los modelos más grandes no mostraron mejoras adicionales e incluso experimentaron ligeras caídas de desempeño\footnote{Cabe destacar que, en rangos superiores a los mencionados, los modelos que han sido factibles para la evaluación (debido a los tamaños de estos modelos) se presentan en menor cantidad que los modelos con tama;os menores. Esto no quita que se observe un decaimiento del rendimiento en modelos grandes, pero es importante considerar tambien la posibilidad de que simplemente la muestra sea poco representativa}. Este fenómeno coincide con observaciones por parte de la organización con respecto a los trabajos presentados, que sugieren que existe un umbral en el tamaño de los modelos para tareas específicas, tras el cual el aumento de parámetros no garantiza un mejor rendimiento.

% La evaluación Zero-Shot permitió identificar los diez modelos más prometedores para la tarea. Los valores de \gls{MAP} obtenidos por estos candidatos en el conjunto de validación justifican su selección para la fase de ajuste fino. La predominancia de modelos multilingües en este grupo refuerza la idea de que es necesario aprovechar representaciones lingüísticas amplias incluso en escenarios monolingües muy especializados.

% \section{Resultados del ajuste fino supervisado}

% Los diez modelos seleccionados se sometieron a un proceso de ajuste fino supervisado utilizando el conjunto de entrenamiento específico de la tarea. Este ajuste continuó el entrenamiento de cada modelo con la función de pérdida MNRL, limitando la ejecución a una sola época con una tasa de aprendizaje muy baja ($5\times10^{-6}$). Esta configuración de entrenamiento fue elegida para evitar el rápido sobreajuste observado al extender el número de épocas, tal como evidencian las curvas de pérdida de validación presentadas en la Figura correspondiente.

% Tras el ajuste fino, se observó que la mayoría de los modelos mejoró su puntuación MAP en el conjunto de validación, con incrementos del orden de 3 puntos porcentuales en promedio. Este resultado valida la utilidad del ajuste supervisado para adaptar los modelos al dominio específico de la tarea. No obstante, la mejora absoluta fue moderada en comparación con el desempeño inicial. Esto sugiere que los modelos preentrenados ya capturaban gran parte de la semántica relevante de los títulos académicos y que el conjunto de datos de entrenamiento disponible proporcionó información adicional limitada.

% Es importante destacar el caso de los modelos \textit{jina-embeddings-v3} y \textit{Linq-Embed-Mistral}, cuyos valores de MAP permanecieron inalterados tras el intento de ajuste. La razón es que estas arquitecturas están diseñadas para inferencia estática y no permiten un reentrenamiento directo en las condiciones del experimento. Además, el modelo \textit{gte-multilingual-base}, que ocupaba la novena posición en la evaluación Zero-Shot, no mostró una mejora significativa con el ajuste fino y aun así fue seleccionado en el sistema final. Esto indica que podrían existir otros modelos no incluidos inicialmente con potencial de mejora tras el ajuste fino.

% Limitar el ajuste fino a los diez mejores modelos fue una decisión motivada por las restricciones de recursos, pero sugiere que, en futuras iteraciones, podría ser beneficioso ampliar el conjunto de candidatos o iterar sobre una selección más diversa. De igual modo, aumentar el tamaño o la diversidad del conjunto de entrenamiento (por ejemplo, mediante recopilación de más ejemplos o técnicas de ampliación de datos) podría permitir entrenar los modelos con más profundidad y reducir el sobreajuste, posiblemente logrando mejoras mayores en el rendimiento.

% \section{Fusión de rankings}

% Tras el ajuste fino se seleccionaron los tres modelos con mejor desempeño (en términos de MAP en validación) para construir un sistema ensamblado mediante fusión de rankings. Estos modelos fueron \textit{bilingual-embedding-large}, \textit{multilingual-e5-large-instruct} y \textit{gte-multilingual-base}. El objetivo de esta fusión fue explotar la complementariedad entre las distintas representaciones aprendidas por cada modelo.

% Se evaluaron tres técnicas de fusión de rankings: Reciprocal Rank Fusion (RRF), Mean Normalized Score (MNZ) y Weighted Sum (WSum). Con la configuración óptima (por ejemplo, suavizado $k=5$ para RRF) se observaron mejoras respecto a los modelos individuales: RRF alcanzó un MAP de 0.4972, MNZ obtuvo 0.4989 y Weighted Sum 0.4995. Esta última constituyó el mejor resultado de la fase, por lo que fue elegida para la propuesta final.

% Sin embargo, la ganancia absoluta fue inferior a un punto porcentual respecto al mejor modelo individual. Esto implica que los modelos combinados comparten en gran medida fortalezas y debilidades. De hecho, dos de los modelos tenían una arquitectura base similar (MPNet), lo cual podría haber limitado la complementariedad. Para maximizar el beneficio de la fusión, sería aconsejable ensayar combinaciones con más modelos o con arquitecturas diferentes, incluyendo a veces modelos con desempeño individual ligeramente inferior que aporten diversidad.

% En general, estas técnicas de ensamblaje demostraron ser válidas, confirmando que la combinación ponderada de representaciones distintas puede mejorar el rendimiento final, aunque con ganancias marginales en este caso específico.

% \section{Reordenamiento (Re-Ranking) de candidatos}

% La fase de re-ranking aportó la mejora más significativa al sistema final. En esta etapa se emplearon modelos \textit{cross-encoder} multilingües para reordenar las listas de candidatos generadas por la fusión. Se evaluó un conjunto de 72 modelos (de una lista inicial de 90) debido a restricciones de recursos y compatibilidad, considerando distintos tamaños de lista (10, 25, 50 y 100 candidatos). Los resultados indican que las mayores ganancias en MAP se concentran al reordenar listas cortas de candidatos (especialmente de 5 o 10 mejores).

% Del análisis comparativo, el modelo \textit{CrossEncoder-ModernBERT-base-qnli} resultó ser el más efectivo. Al reordenar las cinco mejores candidaturas con este modelo, se alcanzó un MAP de 0.5419. Esto representa un aumento de más de 4 puntos porcentuales con respecto al mejor resultado de la fase de fusión. Este importante incremento evidencia que los modelos cross-encoder capturan de manera más precisa la relevancia al procesar conjuntamente la consulta y cada documento, refinando el ranking de salida.

% Cabe destacar que la búsqueda de modelos de re-ranking enfrentó limitaciones: no existe un ranking público específico de cross-encoders para la tarea, y muchos modelos en repositorios como Hugging Face carecen de etiquetas precisas (algunos cross-encoders podrían estar mal clasificados). Por tanto, es posible que se hayan dejado fuera modelos potencialmente útiles. En futuros trabajos se podrían utilizar comparativas dedicadas o etiquetas más fiables, así como explorar técnicas avanzadas (por ejemplo, ensamblar múltiples cross-encoders o incorporar retroalimentación de relevancia) para seguir mejorando el orden final de los resultados.

% A pesar de estas limitaciones, la mejora obtenida en la etapa de re-ranking confirma su importancia en el sistema. Es de esperar que futuras investigaciones continúen incorporando estrategias de re-ranking cada vez más sofisticadas para incrementar la precisión de los resultados finales.

% \section{Limitaciones y aspectos a mejorar}

% El enfoque propuesto, aunque efectivo en la tarea considerada, presenta varias limitaciones. En primer lugar, la dependencia de modelos preentrenados implica heredar los sesgos de sus datos originales, lo cual puede limitar la generalización a dominios distintos al académico. Además, la falta de modelos de entrenamiento contrastivo exclusivos en español obligó a utilizar modelos multilingües, que podrían no captar completamente las particularidades del idioma y los temas específicos de la tarea.

% Desde el punto de vista computacional, el pipeline completo es costoso en tiempo y recursos. La evaluación exhaustiva de decenas de modelos y las etapas posteriores requieren hardware potente y tiempos largos de cálculo. En este trabajo se utilizaron recursos limitados (por ejemplo, una GPU con memoria restringida), lo que condujo a descartar modelos de gran tamaño por inviabilidad práctica. Para aplicaciones en tiempo real o con grandes volúmenes de datos, sería necesario optimizar la arquitectura (por ejemplo, empleando técnicas de distilación o cuantización) o paralelizar los procesos para reducir la complejidad y el tiempo de respuesta.

% La cantidad y diversidad de datos de entrenamiento también es un factor crítico. El conjunto de datos original, aunque apropiado para la tarea, es relativamente pequeño. La falta de ejemplos suficientes contribuye al sobreajuste observado. Como mejora futura, se recomienda ampliar el conjunto con datos adicionales (por ejemplo, generados sintéticamente o procedentes de otras colecciones académicas) y aplicar técnicas de aumento de datos. Esto podría permitir entrenar los modelos con más profundidad y mejorar su robustez.

% Otro aspecto a mejorar es la estrategia de combinación de modelos. En este trabajo se fusionaron solo tres modelos ajustados, dos de los cuales compartían la misma arquitectura base. Esto sugiere que sería beneficioso explorar combinaciones de un número mayor de modelos o de arquitecturas heterogéneas, incluso incluyendo algunos con rendimiento individual inferior pero complementarios. Del mismo modo, la fase de re-ranking podría beneficiarse de un proceso más sistemático de selección y ajuste de modelos, así como de investigaciones sobre ensamblajes de cross-encoders.

% Además, la dependencia de la biblioteca \texttt{SentenceTransformers} para la evaluación inicial limitó la disponibilidad de modelos a aquellos compatibles con esa plataforma. En etapas posteriores, al cambiar a modelos cross-encoder, fue necesario recurrir directamente a repositorios externos. Una futura línea de trabajo sería implementar una mayor integración entre herramientas o desarrollar un mecanismo automatizado para explorar repositorios de modelos, asegurando una cobertura más completa de opciones disponibles.

% En conjunto, estas limitaciones indican que, si bien el sistema desarrollado es competitivo, existe margen de mejora en la diversificación de modelos, en el enriquecimiento de los datos y en la optimización de los recursos computacionales.

% \section{Implicaciones del trabajo}

% Los resultados obtenidos demuestran que una arquitectura que combina múltiples modelos preentrenados con técnicas de fusión y re-ranking puede alcanzar un alto desempeño en tareas de recuperación de información en español. En particular, se confirma que la fase de re-ranking basada en modelos \textit{cross-encoder} es crucial para elevar significativamente la precisión final. Por tanto, este trabajo aporta evidencia empírica de que los enfoques híbridos (mezclando bi-encoders y cross-encoders) son adecuados para escenarios similares en español.

% A nivel práctico, el sistema desarrollado puede servir de base para motores de búsqueda académica en español, siempre que se consideren las limitaciones identificadas. Por ejemplo, en aplicaciones reales sería necesario optimizar los tiempos de respuesta (a través de estrategias de indexación y almacenamiento en caché) y actualizar periódicamente los índices con nuevos documentos. También podría incorporarse retroalimentación de usuario para refinar los modelos en servicio.

% Adicionalmente, las observaciones sobre la necesidad de modelos multilingües efectivos sugieren que la comunidad de investigación debería enfocar esfuerzos en desarrollar y liberar modelos contrastivos de alta calidad en español. La carencia actual de tales recursos representa una oportunidad de contribución. De igual manera, las limitaciones encontradas en el pipeline (por ejemplo, cobertura de modelos y datos limitados) brindan directrices claras: ampliar las fuentes de datos, explorar arquitecturas heterogéneas y desarrollar técnicas de ensamblado más avanzadas. Estas indicaciones servirán para que futuros sistemas ofrezcan mayor precisión y robustez.

% En resumen, los hallazgos de esta discusión resaltan la importancia de la diversidad de modelos y de la estrategia de re-ranking en la recuperación de información, y proporcionan una guía para mejorar y adaptar sistemas similares en futuras investigaciones.